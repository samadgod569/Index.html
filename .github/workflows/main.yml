name: Deploy to Cloudra

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Cloudra Worker
        run: |
          curl -X POST https://code-mon.codemon.workers.dev/cloudra/deploy \
            -H "Content-Type: application/json" \
            -d '{
              "username": "c69p2w",
              "password": "Samadgod169",
              "appName": "app1",
              "appPassword": "samad",
              "repo": "https://github.com/samadgod569/Index.html.git",
              "language": "python",
              "startFile": "main.py",
              "packages": ["flask"]
            }'
